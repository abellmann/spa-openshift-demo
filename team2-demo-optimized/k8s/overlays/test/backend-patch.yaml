apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-team2
spec:
  template:
    spec:
      containers:
        - name: backend
          imagePullPolicy: Always
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "openshift"
